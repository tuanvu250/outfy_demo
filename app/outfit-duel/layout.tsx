import { PhoneFrame } from "@/components/layout/PhoneFrame";

export default function OutfitDuelLayout({ children }: { children: React.ReactNode }) {
    return <PhoneFrame>{children}</PhoneFrame>;
}
